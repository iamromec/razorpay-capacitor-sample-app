# razorpay-capacitor-sample-app

To install dependencies

`npm i`

To build and run

`ionic capacitor add android`

`ionic capacitor add ios`

`ionic capacitor build android`

`ionic capacitor build ios`
